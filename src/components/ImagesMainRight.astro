---
const { src, alt, class: className = '' } = Astro.props;
---

<div class={`absolute ${className}`}>
  <img
    src={src}
    alt={alt}
    class="w-30 drop-shadow-2xl drop-shadow-purple-600/10"
  />
</div>
