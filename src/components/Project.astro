---
const { src, alt, class: className = '' } = Astro.props;
---

<div
  class={`group ${className} col-span-6 relative overflow-hidden row-span-5 h-80 rounded-lg border shadow-gray-900 shadow-md border-gray-200/40 hover:border-gray-100/90 bg-gradient-to-br from-slate-900 to-slate-800 transition-all`}
  tabindex="0"
>
  <img
    src={src}
    alt={alt}
    class="absolute inset-0 h-full w-full rounded-lg transition-transform ease-in-out group-hover:scale-105 group-focus-within:scale-105"
  />

  <div
    class="relative z-10 flex flex-col justify-end h-full w-full p-6 bg-gradient-to-t from-slate-950/50 via-slate-950/30 to-transparent"
  >
    <slot />
  </div>
</div>
