---
const { src, alt, class: className = '' } = Astro.props;
---

<div class={`absolute ${className}`}>
  <img
    src={src}
    alt={alt}
    class="my-image-left w-30 drop-shadow-2xl drop-shadow-purple-700/20 my-animate opacity-55"
  />
</div>

<script>
  const floatImages = document.querySelectorAll('.my-image-left');

  const moveImages = () => {
    floatImages.forEach((image) => {
      const x = Math.random() * 100 - 30;
      const y = Math.random() * 100 - 30;

      //@ts-ignore
      image.style.transform = `translate(${x}px, ${y}px)`;
    });
  };

  moveImages();

  setInterval(moveImages, 3000);
</script>
